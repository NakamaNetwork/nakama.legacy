export function configure(config) {
    config.globalResources([
        './alert-dialog',
        './ship-picker',
        './stage-picker',
        './team-import',
        './unit-picker'
    ]);
}