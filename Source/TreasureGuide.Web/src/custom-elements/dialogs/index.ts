export function configure(config) {
    config.globalResources([
        './alert-dialog',
        './ship-picker',
        './stage-picker',
        './report-dialog',
        './team-import',
        './unit-picker'
    ]);
}