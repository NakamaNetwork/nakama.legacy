export function configure(config) {
    config.globalResources([
        './age-format',
        './currency-format',
        './date-format',
        './datetime-format',
        './time-format'
    ]);
}